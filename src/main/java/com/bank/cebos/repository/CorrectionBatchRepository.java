package com.bank.cebos.repository;

import com.bank.cebos.entity.CorrectionBatch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CorrectionBatchRepository extends JpaRepository<CorrectionBatch, Long> {}
